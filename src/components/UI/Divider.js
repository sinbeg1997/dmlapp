import React from "react";
import { View } from "react-native";

const Divider = (props) => (
    <View
        style={{
            height: 1,
            width: '100%',
            backgroundColor: 'rgba(70,61,49,.25)',
            borderStyle: "dashed"
        }}
    />
)

export default Divider;